=== SOLUÇÃO TEMPORÁRIA - COMENTAR VIEWS DE WHATSAPP ===

1. No arquivo views.py, comente ou remova o import:
   # from .whatsapp_service import WhatsAppService

2. Comente as duas views:
   # @login_required(login_url='login')
   # def recorrencia_enviar_boleto_whatsapp(request, pk):
   #     ... (toda a função)
   
   # @login_required(login_url='login')
   # def recorrencia_enviar_link_whatsapp(request, pk):
   #     ... (toda a função)

3. No arquivo urls.py, comente as rotas:
   # path('recorrencias/<int:pk>/enviar-boleto-whatsapp/', views.recorrencia_enviar_boleto_whatsapp, name='recorrencia_enviar_boleto_whatsapp'),
   # path('recorrencias/<int:pk>/enviar-link-whatsapp/', views.recorrencia_enviar_link_whatsapp, name='recorrencia_enviar_link_whatsapp'),

4. No template list.html, comente a seção de WhatsApp:
   <!-- Ações de WhatsApp -->
   <!--
   <div class="flex justify-end space-x-2 pb-2 border-b border-gray-200">
       ...
   </div>
   -->

5. Reinicie o servidor:
   sudo systemctl restart gunicorn
   sudo systemctl restart nginx

Isso vai fazer o sistema voltar a funcionar sem as features de WhatsApp.
Depois você pode adicionar o whatsapp_service.py e descomentar.
